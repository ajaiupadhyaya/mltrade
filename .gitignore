.worktrees/
.DS_Store
.env
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
dist/
build/
*.egg-info/
data/
artifacts/
mlruns/
*.db

# The dashboard's exported JSON is a committed, deterministic fixture-derived
# artifact (regenerate with `mltrade export`), not runtime state. Re-include it
# even though the generic `data/` rule above would otherwise exclude it.
!web/public/data/
